# Web フレームワーク
flask
flask-cors
flask-limiter
python-dotenv
gunicorn

# レート制限の共有ストア（flask-limiter が Redis URI から自動使用）
redis

# セキュリティ
defusedxml

# LLM クライアント（ロケーション GPS 推定）
openai>=1.40
pyyaml

# Azure Cosmos DB（オプション: JSON フォールバックを使う場合は不要）
azure-cosmos
azure-identity

# Azure Monitor / Application Insights（オプション: 未設定時はスキップ）
azure-monitor-opentelemetry
