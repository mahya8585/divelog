# Azure Cosmos DB 接続設定（オプション）
# 設定しない場合は workflow/json/ のJSONファイルを直接読み込みます

COSMOS_ENDPOINT=https://<your-account>.documents.azure.com:443/
COSMOS_KEY=<your-primary-key>
COSMOS_DATABASE=divelog
COSMOS_CONTAINER=dives
COSMOS_ZXU_CONTAINER=zxu_uploads

# ユーザー管理用コンテナ（デフォルト: "users"）
COSMOS_USERS_CONTAINER=users
# トークン管理用コンテナ（デフォルト: "tokens"、TTL = 10 分）
COSMOS_TOKENS_CONTAINER=tokens

# ── ログイン認証設定 ──────────────────────────────────────
# Cosmos DB が設定されている場合:
#   初回起動時に AUTH_EMAIL / AUTH_PASSWORD を使って users コンテナにユーザーを自動作成します。
#   以降はユーザーデータは Cosmos DB で管理されます。
# Cosmos DB が設定されていない場合（ローカル開発向けフォールバック）:
#   AUTH_EMAIL / AUTH_PASSWORD による直接比較で認証します。
AUTH_EMAIL=your@email.com
AUTH_PASSWORD=your-password

# トークン署名用シークレットキー（Cosmos DB 未使用時のフォールバック用）
# 本番環境では必ず固定値を設定してください。
SECRET_KEY=your-secret-key
